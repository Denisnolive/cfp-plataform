# share-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build share-types` to build the library.

## Running unit tests

Run `nx test share-types` to execute the unit tests via [Jest](https://jestjs.io).
